<h1>New Category</h1>
<%= link_to 'Back', admin_categories_path, :class => "pull-right  btn btn-primary" %>

<%= nested_form_for([current_admin, @category], :html => { :multipart => true }) do |f|%>
 <%= render :partial => 'form', :locals => {:f => f} %>
 <% end %>