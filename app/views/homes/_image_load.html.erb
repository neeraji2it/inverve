<% if params[:action] == "load_image" %>
	<div class="product-img-box" style="margin-bottom: 60px;">
		<%= image_tag(@image.avatar.url, height: "200px", width: "275px") %>
	</div>
<% else %>
	<div class="product-img-box" style="margin-bottom: 60px;">
		<%= image_tag(product.images.present? ? (product.images.first.avatar.url) : "/assets/missing.png", height: "200px", width: "275px") %>
	</div>
<% end %>